[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  # Next.js 15.5 requires Node ^18.18 / ^20. Pin explicitly so Netlify's
  # build image doesn't silently fall back to an older default.
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
